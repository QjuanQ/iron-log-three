# Iron Log Three

This project is a separate copy of the Iron Log app, with its own identity and deployment path.

- Local directory: `iron-log-three`
- Intended remote: `https://github.com/QjuanQ/iron-log-three.git`
- Previous shared project remote is kept as `upstream` for reference:
  - `https://github.com/QjuanQ/iron-log.git`

This copy uses `vite.config.js` base path `/iron-log-three/` and package name `iron-log-three`.
